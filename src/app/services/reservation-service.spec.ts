import { ReservationService } from './reservation-service';

describe('ReservationService', () => {
  it('should create an instance', () => {
    expect(new ReservationService()).toBeTruthy();
  });
});
